# Rmpi test cases

-   `Spawn*` subdirectories: These are examples that use `mpi.spawn.Rslaves` which 
    cannot work on LUIMI as spawning additional MPI processes is not supported in
    Cray MPICH.

