# Testing the Rust modules

1.  Simple test: Compile the `hello.rs` program in the `Hello` subdirectory:
    
    ```
    rustc hello.rs
    ./hello
    ```
    
2.  Create an EasyConfig for HyperQueue and install.

