# Changes in Lmod

-   8.7.12: User cache directory changed from `~/.lmod.d/.cache` to `~/.cache/lmod`.


7.8.9 used spiderT.x86_64_Linux.lua and stores a compiled version also. The spider 
command also already had spiderT as output style. And so is the case in 7.8.8.


